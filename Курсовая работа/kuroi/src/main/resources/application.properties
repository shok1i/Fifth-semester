# Работа с mySQL
spring.datasource.url=jdbc:mysql://localhost:3306/kuroi
spring.datasource.username=root
spring.datasource.password=root
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Настроенный порт для нашего веб-приложения
server.port=3060

# Включение _csrf токена для защиты (что бы это не было)
spring.freemarker.expose-request-attributes=true